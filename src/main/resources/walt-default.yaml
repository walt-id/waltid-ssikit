hikariDataSource:
    jdbcUrl: jdbc:sqlite:data/walt.db
    maximumPoolSize: 5
    autoCommit: false
    dataSource:
        journalMode: WAL
        fullColumnNames: false

azureKeyVaultConfig:
    baseURL:
    id:
    secret:

velocityConfig:
    agentApiVersion: "v0.8"
    registrarApiVersion: "v0.6"
    agentEndpoint: "http://127.0.0.1:8081"
    registrarEndpoint: "https://stagingregistrar.velocitynetwork.foundation/"
